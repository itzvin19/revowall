export type Proyecto = {
	title: string;
	img: string;
	caracteristicas: string[];
};

export type Testimonio = {
	nombre: string;
	trabajo: string;
	testimonio: string;
};

export type CasoExito = {
	title: string;
	img: string;
	description: string;
};